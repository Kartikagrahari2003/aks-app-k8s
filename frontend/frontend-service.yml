apiVersion: v1
kind: Service

metadata:
  name: aks-app-frontend-service
  namespace: aks-app

spec:
  selector:
    app: aks-app-frontend

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

  type: LoadBalancer